// This is a mock Firebase service to avoid actual Firebase dependencies

// Mock Firebase Auth
export const auth = {
  // Empty implementation
};

// Mock Firestore
export const db = {
  // Empty implementation
};

// Mock Storage
export const storage = {
  // Empty implementation
};

// Mock Firebase app
const app = {
  // Empty implementation
};

export default app; 